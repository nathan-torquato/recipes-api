version: '3'

services:
  app:
    container_name: recipes-api
    restart: always
    build: .
    command: npm start
    environment:
      RECIPE_PUPPY_API_URL: 'http://www.recipepuppy.com/api'
      GIF_PUPPY_API_URL: 'http://api.giphy.com/v1/gifs/search'
      GIF_API_KEY: ''
    ports:
      - '7777:3000'
